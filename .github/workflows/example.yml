name: Example Action

on:
  push:
    branchs : [testing]

jobs:
  example:
    runs-on: ubuntu-latest
    steps:
      - name: Print
        run: echo "Hello World"
      - name: Print ( Debug )
        run: echo "::debug::This is a debug message"